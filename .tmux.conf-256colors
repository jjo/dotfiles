# Needed for more colorful colorscheme-s, like sonokai, gruvbox, sublimemonokai
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
# Highlight in reverse instead of italic
set -g terminal-overrides 'xterm-256*:sitm@,ritm@'
